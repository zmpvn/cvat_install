# Install CVAT on Ubuntu:

  ```
  curl -sSL https://github.com/zmpvn/cvat_install/raw/master/ubuntu1.sh | bash
  
  #Logout and login
  
  curl -sSL https://github.com/zmpvn/cvat_install/raw/master/ubuntu1.sh | bash
  ```
